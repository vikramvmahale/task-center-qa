import { test } from '@playwright/test';

/**
 * Placeholder suite to keep the `tests/functional` folder in place.
 *
 * This file is intentionally non-functional and should not contain
 * any real tests. The entire suite is skipped so it will not run.
 *
 * When adding real functional tests, you can either:
 * - Replace this file, or
 * - Delete it once at least one real functional spec exists.
 */
test.describe.skip('Placeholder Functional Test', () => {
  // Intentionally empty
});
